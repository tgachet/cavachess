{% extends 'layout.html.twig' %}

{% block page_title %}<h1>CAVACHESS </h1>
    <h3> Jouez aux échecs en ligne contre les meilleurs ! </h3>{% endblock %}

{% block content %}
    {% if app.user is null %}
        <div class="row">
            <div class ="coverfront col-sm-7" style="border:solid black 1px;" class="img-responsive" alt="King Edinson Cavani" >
                <img src="{{ asset('img/cavani.jpg') }}" />
            </div>
            <div class="blocfront col-sm-5" style="border:solid black 1px; min-height: 320px">
                <div class="boutonshome col-sm-8 col-sm-offset-2" style="margin-top:50px;">
                    <div style="background-color: menu">
                        <b>Il y'a actuellement {{ cons | length }} joueur(s) connecté(s)</b>
                    </div><br>
                    <a href="{{ path('login') }}">
                        <button type="button" class="btn btn-primary btn-lg btn-block">Se connecter</button>
                    </a>
                    <a href="{{ path('app_security_register') }}">
                        <button type="button" class="btn btn-default btn-lg btn-block">S'inscrire</button>
                    </a>
               </div>
            </div>
        </div>
    {% else %}   
        <div class="row">
            <div class ="coverfront col-sm-6">
                
            </div>
            <div class="blocfront col-sm-6">

            </div>
        </div>
    {% endif %}
    <h2>Article le plus populaire</h2>
    <div class="row">
        <div class="col-sm-9">
            {% include 'post/toppreview.html.twig' with {"post" : top_article, "comments" : comments} %}
        </div>
    </div>
    <h2>Derniers articles</h2>
    <div class="row">
        <div class="col-sm-9">
            {% for post in posts %}
                {% include 'post/preview.html.twig' with {"post" : post} %}
            {% endfor %}
        </div>
        <div class="col-sm-3">
            <div class="row">
                {% if app.user is not null %}
                <div class="col-sm-11 col-sm-offset-1">
                    <div style="background-color: moccasin; padding: 1em;">
                        <b>{{ cons | length }} joueur(s) connecté(s)</b>
                    </div>
                    <div style="background-color: lemonchiffon; text-align: center;">
                    {#{{ dump(cons) }}#}
                    {% for con in cons %}
                        {% if app.user.id == con.id %}
                            <br/><a href="{{ path('app_user_displayinfo', {"id": con.id}) }}">---- Moi ----</a>
                        {% else %}
                        <br/><a href="{{ path('app_user_displayinfo', {"id": con.id}) }}">{{con.username}}</a>
                        {% endif %}
                    {% endfor %}
                </div>
                {% endif %}
            </div>
        </div>
    </div>
{% endblock %}